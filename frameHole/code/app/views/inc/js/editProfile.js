/*

document.addEventListener("DOMContentLoaded", function(event) {
    var para = document.getElementById('email');

    para.addEventListener('click', updateName);

    function updateName() {
        var name = prompt('Enter a new name');
        para.textContent = 'Player 1: ' + name;
    }
});*/
